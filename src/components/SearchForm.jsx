import { Input } from "reactstrap";
const SearchForm = () => {
    return(
        <>
            <Input onClick={() => alert ('Soy un Input') }/>
            Icon //
        </>
    );
}

export default SearchForm;